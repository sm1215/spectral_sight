use spectral_sight::*;

#[test]
fn test_wow_path() {
    set_wow_path();
    // assert_eq!("sample", output);
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}